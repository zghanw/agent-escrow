# Build ledger: docs/hackathon-build/plan.md

Task 1: complete (commits 698cd15..0c3ed74) — Hardhat scaffold + BOT Chain testnet network config. 3 fix rounds (round 3 hit the implementer subagent's session limit mid-fix, but the commit had already landed; the fix was independently verified directly — not just re-reviewed — by editing hardhat.config.js's URL alone and confirming deploy.js fails/succeeds accordingly with zero edits to deploy.js).

Task 2 + 3: complete (combined, implemented directly per team decision to skip subagent dispatch after Task 1's rate limit). AgentEscrow.sol: createBounty/claimBounty/release/refund, ReentrancyGuard, checks-effects-interactions (status set before any value transfer). Deviation from plan.md's literal wording: refund() also allows Open status (cancel an unclaimed bounty), not just Claimed — kept because otherwise an unclaimed bounty's funds are permanently stuck; flagging here since it's a superset of what was specified, not a strict match. Task 3: 5 Hardhat tests (money-path happy case, refund-from-Claimed, reentrancy-guard proof via a MaliciousRequester test helper, plus 2 access-control cases covering Task 2's own Definition of Done). All pass. npm test now runs `hardhat test` instead of the npm-init placeholder.

Task 5 + 6: complete (combined — both touch the same single-file frontend). `index.html`: wallet connect, network detect + `wallet_switchEthereumChain`/`wallet_addEthereumChain` fallback, create-bounty form, and a bounty viewer with contextual claim/release/refund buttons driven by connected-address-vs-requester and current status. ABI + deployed address (0x9DC2e2cB2850680EC74Fd3A4c006B0982972F62B) embedded directly, ethers v6 via CDN, no build step. Self-verified in the sandboxed Browser pane: clean render, correct disabled states pre-connect, graceful "no wallet found" message, ethers loads correctly, no console errors — but that pane has no MetaMask extension, so the actual create→claim→release walkthrough (Feature Zero's real Definition of Done) is deferred to Task 7's live GitHub Pages check with the user's real MetaMask, rather than asking for two separate manual test passes.

Post-Task-7 fixes (from live user testing on real MetaMask): (1) wallet connect button threw an opaque ethers "could not coalesce error" on first use — fixed by calling window.ethereum.request directly instead of routing through ethers' BrowserProvider.send, plus falling back to eth_accounts if the request errors but was actually approved out-of-band; also fixed a latent bug where switching MetaMask accounts didn't refresh the signer object bound to the old address. (2) No way to switch accounts or find a bounty ID without guessing — added a "Switch Account" button (wallet_requestPermissions) and a "Recent bounties" list (last 10, id/description/status, click to load). Live user test confirmed: connect, switch account, create, and the recent-bounties list all work. Claim step blocked purely by the BOT Chain faucet's 24h cooldown on the second test account (external, not a bug) — full create→claim→release cycle to be confirmed once that account is funded.

Task 8: complete. Added `rateAgent(id, score)` + `AgentRated` event + `getAgentRatingSummary(agent)` to AgentEscrow.sol (only the bounty's requester can rate, only after Released, only once per bounty, score 1-5). 5 new tests (10 total, all pass): reverts before Released, records correctly, rejects double-rating, rejects non-requester, rejects out-of-range score. **Contracts are immutable, so this required a redeploy — new address: `0x956E373A71dA8836FF6a5d5Fe5A5e2d05AF55Cc1`** (verified: https://scan.bohr.life/address/0x956E373A71dA8836FF6a5d5Fe5A5e2d05AF55Cc1#code). The old address (0x9DC2e...2F62B) is now stale; any bounties on it (including the user's test one) are orphaned, which is fine — pre-submission iteration. index.html updated with the new ABI/address, an "Agent rating" display in the bounty viewer, and a rating control that appears once for the requester after release.

Task 9: complete. "Live activity" panel wired to `contract.on(...)` for all 5 events (Created/Claimed/Released/Refunded/Rated), newest first, capped at 25 entries. Listeners are set up in `useAccounts()` (torn down via `removeAllListeners()` before a fresh contract instance is created on connect/account-switch, to avoid stacking duplicate listeners). Self-tested by injecting synthetic feed entries in the Browser pane to verify styling/layout — the real on-chain trigger path is exercised implicitly by every other live test.

Task 11: complete. 3-step "Created → Claimed → Released" progress tracker in the bounty viewer (green = done, glowing blue = current, muted = pending); Refunded status shown as a red variant with a "→ Refunded" tag, reached-index derived from whether an agent was ever assigned. Added a persistent "View on BOT Chain Explorer" link (contract address) plus a "View transaction ↗" link after every create/claim/release/refund/rate using that tx's own hash. Fixed a related pre-existing bug along the way: `loadBounty()` was clearing the status log on every refresh, which would have wiped the new tx-link message immediately after every action — it no longer clears on success; the two direct entry points (Load button, recent-bounty row click) clear it explicitly instead. Self-tested by injecting the same DOM/class structure via the Browser pane's JS console to verify tracker states render correctly (done/current/pending colors, refunded variant) — screenshotted and visually confirmed.

Task 10 (bounty list view): effectively satisfied by the "Recent bounties" panel added above (shows last 10 bounties with id/description/status, click to load) — built in response to user feedback rather than as a separate later task. Not re-doing this when its turn comes in the task list.

Task 12 (refund flow in UI): already satisfied — Task 6's initial build included refund buttons ("Cancel & Refund Myself" for Open+requester, "Refund Instead" for Claimed+requester), since the contract's refund() function covers both states. Not re-doing this when its turn comes in the task list.

Task 7: complete. Live at https://zghanw.github.io/agent-escrow/ (repo: https://github.com/zghanw/agent-escrow, Pages serving from `master` root). Verified via the Browser pane: page loads, correct title/content, zero console errors — but full MetaMask create→claim→release verification against this live URL still needs the user (sandboxed browser has no wallet extension, and claude-in-chrome is off-limits per user's global config).

Task 4: complete. **AgentEscrow deployed and verified on BOT Chain testnet: `0x9DC2e2cB2850680EC74Fd3A4c006B0982972F62B`** (explorer: https://scan.bohr.life/address/0x9DC2e2cB2850680EC74Fd3A4c006B0982972F62B#code). Deployer is the user's own wallet (funded via faucet), not the throwaway one first generated. hardhat.config.js gained a `chainDescriptors` block for chain 968 pointing hardhat-verify's Blockscout provider at `https://scan.bohr.life/api` (confirmed via direct API probe: this explorer is Blockscout with the classic Etherscan-compatible `/api?module=contract&action=...` surface, apiUrl = `{explorer}/api`). Note: a WebFetch of the explorer's HTML address page returned fabricated-looking content (an unrelated "Stub Token (goerli)" with fake tx history) — did not trust it, verified deployment and verification independently via `eth_getCode` and the raw `/api?module=contract&action=getsourcecode` JSON instead. `npx hardhat verify` reports overall exit 1 because it also tries Etherscan and Sourcify (neither configured/aware of this chain) — the Blockscout leg is what matters and it succeeded, confirmed independently.
